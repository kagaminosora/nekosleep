<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xjtlu.eevee.nekosleep">

    <uses-permission android:name="android.permission.VIBRATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".collections.PetScreenSlideActivity">
        </activity>
        <activity android:name=".home.MainActivity"></activity>
        <activity android:name=".settings.UserSettingsActivity">
        </activity>
        <activity android:name=".collections.ui.PetScreenSlideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".home.MainActivity"></activity>
        <activity android:name=".collections.ui.ChooseItemActivity"></activity>
        <activity android:name=".alarm_clock.AlarmClockActivity"></activity>

    </application>

</manifest>